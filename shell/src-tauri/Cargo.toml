[package]
name = "hiapphub-shell"
version = "0.1.0"
edition = "2021"
description = "HiAppHub 极简 Shell — 4000+ 免费工具平台"

[dependencies]
hiapphub-host = { path = "../../hiapphub-host" }

[profile.release]
strip = true
lto = true
codegen-units = 1
opt-level = "z"
panic = "abort"
