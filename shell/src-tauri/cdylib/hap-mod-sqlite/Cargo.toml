[package]
name = "hap-mod-sqlite"
version = "0.1.0"
edition = "2021"
description = "HiAppHub cdylib — SQLite 数据库操作"

[lib]
crate-type = ["cdylib"]

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
serde_json = "1"
